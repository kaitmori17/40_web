# CLAUDE.md — 恒久ルール(プロジェクト憲法)

このファイルは「変わらないルール」だけを軽量に保つ。進捗・学習ログは ROADMAP.md と各サイトの status.md に書く。

## 目的とゴール
- コンサルタント(専門: 人事・組織・経営)が、AIを活用して Web サイトをゼロから設計・構築・公開し、SEO までを一通り実践して理解する。
- 最終ゴール: **Google AdSense のダッシュボードに広告収益が計上されること**(金額は問わない/受け取り自体は目的でない)。
- 収益額より **各工程の技術と概念の理解を最優先** する。

## 学習スタイル(進め方の鉄則)
- 専門用語は **必ず先に平易に説明してから** 使う。SEO・Web公開の前提知識はゼロと想定する。
- 全自動で済ませない。**概念を説明 → 本人が理解・判断 → 実践** の流れで、一区切りずつ進める。
- 各レイヤーの区切りで、理解できたかを本人に確認してから次へ進む。
- 出てきた専門用語は GLOSSARY.md に都度追記して育てる。ユーザー自身が発した技術的な質問に答えたら、その要点も GLOSSARY.md に残す(自発的な補足だけのものは無理に残さない)。

## セッション運用(開始時・終了時)
10_app プロジェクトと同じ運用に揃える。

### 対話開始時(必須)
1. 管理リポジトリ(40_web)と作業対象サイトのリポジトリで `git status` を確認し、未コミットの変更があれば内容を報告する(前回の取りこぼし防止)。あれば commit & push を提案する。
2. 作業対象サイトの `status.md`(なければ ROADMAP.md)を読んで状態を復元する。
3. 現状と「次のアクション候補」を説明し、何から始めるか提案してから着手する。

### 対話終了時(必須)
1. セッション中の変更を確認し、未コミットがあれば commit & push する(作業消失防止)。
2. その日に出た専門用語・技術トピックを GLOSSARY.md、進捗を ROADMAP.md と該当 status.md に追記してからコミットする。
3. ユーザーが終了を告げたとき・作業の区切りで実行する。先に会話が閉じられることもあるため、節目ごとにも随時 commit & push を促す。

**プッシュはコミットと同時に行う(溜めない)。**

### コミットメッセージ形式
`[種別]: [内容]`。種別は feat(新機能) / fix(修正) / design(UI) / docs(ドキュメント) / chore(設定・環境)。

## 環境・運用ルール
- 作業フォルダは OneDrive 配下を避ける。パスは `/` 区切りで扱う。
  - 本プロジェクトの管理ルート: `D:/local_backup_manual/dev/40_web`(OneDrive 外)。
- 状態管理: 各サイトの `status.md` を状態の正本とする。**毎セッション冒頭にまず status.md を読んで状態を復元** する。
  - 進捗・学習ログ → ROADMAP.md と status.md に書く。CLAUDE.md は恒久ルールだけの軽量な状態を保つ。
- フォルダ構成(地図は `README.md`):
  - **自分の学習サイトは1つ**=`sites/self`(1リポジトリ。中に複数ハブを増設。複数の独立サイトは作らない)。進捗の正本は ROADMAP.md →(作成後)`sites/self/status.md`。
  - **受託案件は別物**=`sites/clients/<案件>`(案件ごとに独立リポジトリ+自前 status.md)。**ROADMAP/GLOSSARY に受託の詳細を混ぜない**。
- Git / GitHub 管理:
  - 1サイト = 1リポジトリ = 1 Vercelプロジェクト を基本とする。各サイトは独立した Git リポジトリ + GitHub プライベートリポジトリ(バックアップ+履歴+複数PC同期)。
  - 全体管理フォルダ(CLAUDE.md / ROADMAP.md / GLOSSARY.md)も軽量な管理用リポジトリにする。リポジトリの入れ子を避けるため、管理用リポジトリでは `sites/` を `.gitignore` する。
  - `.gitignore` に node_modules、.next、.env*、.vercel、ビルド成果物を必ず含める。
  - 秘密情報(APIキー、.env、認証情報)・機密情報は絶対にコミットしない。
  - 区切りの良いところで、内容が分かるメッセージでコミットする(セーブポイント)。status.md もコミットして PC 間で状態同期。
  - 複数PC間の同期は OneDrive ではなく `git push/pull` で行う。別PCでは `clone` で持ち込む。

## ガードレール(守るべき制約)
- スタックは **Next.js + Vercel + 独自ドメイン**。
- **E-E-A-T は本人の専門性(人事・組織・経営)で担保** する。テーマは専門領域から選ぶ。
- Google の「スケール化されたコンテンツの濫用」を避ける:
  - 記事は量産しない(1サイト **10〜20本の質重視**)。
  - 一度に大量公開せず、人間のペースで。
  - テンプレ流用の薄いページを作らない。
- 診断等の **結果はインデックスさせず動的表示**(薄いテンプレ=doorway ページを作らない)。
- 収益は **広告(AdSense)+ 結果画面の関連アフィリエイト** の2軸。有料商品は当面作らない。
- AdSense 要件: 独自ドメイン必須、記事10本以上、プライバシーポリシー・問い合わせ・運営者情報を用意。
