# CLAUDE.md — 恒久ルール(プロジェクト憲法)

「変わらないルール」だけを軽量に保つ。詳細は下記の専用ドキュメントに置き、ここには**ポインタ**だけ残す。

## 目的とゴール
- コンサルタント(専門: 人事・組織・経営)が、AIを活用して Web サイトをゼロから設計・構築・公開し、SEO までを実践して理解する。
- 最終ゴール: **Google AdSense のダッシュボードに広告収益が計上されること**(金額は問わない)。
- 収益額より **各工程の技術と概念の理解を最優先** する。

## 学習スタイル(進め方の鉄則)
- 専門用語は **必ず先に平易に説明してから** 使う(前提知識ゼロを想定)。
- 全自動にしない。**概念を説明 → 本人が理解・判断 → 実践** の流れで、一区切りずつ進める。各区切りで理解を確認してから次へ。
- 記録の住み分け:**用語 → `GLOSSARY.md`** / **ユーザーが発した技術質問の回答 → `docs/tech-learning-log.md`**(自発的補足は無理に残さない)。

## セッション運用(開始時・終了時)
10_app と同じ運用に揃える。

### 対話開始時(必須)
1. 管理リポジトリ(40_web)と作業対象サイトで `git status` を確認。未コミットがあれば報告し commit & push を提案(取りこぼし防止)。
2. 作業対象サイトの `status.md`(なければ ROADMAP.md)を読んで状態を復元。
3. 現状と「次のアクション候補」を説明し、何から始めるか提案してから着手。

### 対話終了時(必須)
1. セッション中の変更を commit & push(消失防止)。
2. その日の**用語 → GLOSSARY.md**、**技術Q&A → docs/tech-learning-log.md**、**進捗 → ROADMAP.md(作業ログ含む)と該当 status.md** に追記してからコミット。
3. 終了時・作業の区切りで実行。先に会話が閉じることもあるため、**節目ごとにも随時 commit & push** を促す。

**プッシュはコミットと同時に行う(溜めない)。**

### コミットメッセージ形式
`[種別]: [内容]`。種別 = feat(新機能) / fix(修正) / design(UI) / docs(ドキュメント) / chore(設定・環境)。

## ドキュメント構成(どこに何を書くか)
| ファイル | 役割 |
|---|---|
| `README.md` | フォルダ構成の地図 |
| `ROADMAP.md` | 学習計画・進捗・**作業ログ**(自分のサイト中心) |
| `GLOSSARY.md` | 用語の定義 |
| `docs/tech-learning-log.md` | 技術Q&Aの蓄積(文脈つき) |
| `docs/web-development-rules.md` | **Web開発の段階別・制約ルール(ガードレール/公開/AdSense/技術SEO)** |
| `sites/<site>/status.md` | 各サイトの状態の正本(毎セッション冒頭に読む) |
| `sites/self/docs/` | self の記事スタイルガイド・デザインプロンプト・企画シート |
| `.claude/agents/` | 記事生成サブエージェント(調査/執筆/校閲) |

## 環境・Git(要点)
- 作業フォルダは OneDrive 配下を避ける。パスは `/` 区切り。
- **自分の学習サイトは1つ**=`sites/self`(中に複数ハブを増設)。**受託は別**=`sites/clients/<案件>`(独立リポジトリ+自前 status.md。ROADMAP/GLOSSARYに混ぜない)。
- **1サイト = 1リポジトリ = 1 Vercelプロジェクト**。管理リポジトリは `sites/` を `.gitignore`(入れ子回避)。`.gitignore` に node_modules / .next / .env* / .vercel / ビルド成果物。
- **秘密情報(APIキー・.env・認証情報)は絶対にコミットしない**。PC間同期は git push/pull(OneDrive不使用)。

## ガードレール・段階別ルール
スタック・E-E-A-T・スケール化濫用回避・doorway回避・収益2軸・公開/商用ホスティング・AdSense要件・技術SEO は **`docs/web-development-rules.md`** に集約。**実装・公開・収益化の各段階で必ず参照**する。
