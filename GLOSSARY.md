# GLOSSARY.md — 用語集

専門用語が出てくるたびに、平易な説明で追記して育てる。アルファベット/五十音は気にせず、出てきた順でよい。

## Git / リポジトリ管理
- **リポジトリ (repository / repo)**: Git が変更履歴を管理する「1つの箱」。箱ごとにバックアップ・履歴・複数PC同期ができる。
- **コミット (commit)**: 変更内容を履歴に1つの記録として刻むこと。ゲームのセーブポイントのイメージ。
- **push / pull**: ローカルの履歴を GitHub に送る(push)/ GitHub の履歴を取り込む(pull)。複数PC同期に使う。
- **clone**: GitHub 上のリポジトリを別PCに丸ごと複製して持ち込むこと。
- **`.gitignore`**: 「このリポジトリでは、これらのファイルは履歴に含めない」という除外リスト。巨大な自動生成物や秘密情報を入れないために使う。
- **リポジトリの入れ子 (nested repo)**: リポジトリの中に別のリポジトリが入っている状態。Git が混乱するので避ける。本プロジェクトでは管理用リポジトリで `sites/` を無視して回避する。

## ホスティング / 公開
- **サーバ / ホスティング**: サイトのファイルを置いて24時間配信するコンピュータ(と、その場所を貸すサービス)。自前PCではなくホスティングを借りるのが基本。
- **静的サイト / 動的サイト**: 静的=完成済みのHTMLをそのまま配るだけ(速い・安全・安い)。動的=アクセスのたびにサーバが計算してページを作る(会員制・検索等)。
- **Vercel**: GitHubと連携し push で自動デプロイできるホスティング。無料(Hobby)は**非商用向け**、商用はProまたは他社。
- **Cloudflare Pages**: 静的サイト向けの無料ホスティング。**無料でも商用利用OK**。ドメイン購入(Registrar)・DNSも同社で完結でき最安・最簡。
- **Vercel プロジェクト**: Vercel 側での「1サイトの管理単位」。1サイト=1リポジトリ=1Vercelプロジェクト で対応させる。
- **デプロイ (deploy)**: 作ったサイトを公開状態にすること。GitHub連携時は push で自動実行。
- **プレビュー / 本番 (Preview / Production)**: 本番=みんなが見る正式版(通常mainブランチ・独自ドメイン)。プレビュー=作業中の確認用に自動発行される別URL。本番を壊さず試せ、オーナー確認にも使える。
- **CDN**: 世界各地の配信拠点。訪問者に一番近い場所から配って表示を速くする仕組み。
- **HTTPS / SSL**: 通信を暗号化する仕組み(アドレスバーの鍵マーク)。Vercel/Cloudflareが自動で無料発行。
- **noindex / robots メタ**: 検索エンジンに「このページを検索結果に載せないで」と伝える指定。未完成の確認段階で入れ、本番公開時に外す。
- **OGP (Open Graph)**: SNSでURLを共有したとき表示されるタイトル・説明・画像を指定するメタ情報。
- **埋め込み (embed) / ウィジェット**: 外部サービス(X/Instagram等)の内容を自サイト内に表示する部品。Xのタイムライン埋め込みは仕様変更で不安定なことがある。Instagramのフィードは第三者ウィジェット(SnapWidget等)かGraph APIが必要。
- **フォーム送信サービス**: 静的サイトには送信を受けるサーバがないため、問い合わせフォームの内容をメール転送してくれる外部サービス(Web3Forms/Formspree等)。無料枠あり。

## ドメイン / DNS
- **ドメイン**: サイトの住所(例 `example.com`)。レジストラから年額で借りる。ホスティング(建物)とは別物で、引っ越しても住所として持ち運べる=自分の資産。
- **サブドメイン**: ルートドメインの前に名前を足したもの(例 `milimili.example.com`)。1本のドメインから無料・無制限に作れ、別々のサイトに割り当て可能。ただしブランドは独立ルートの方が見栄えが良い。
- **レジストラ (registrar)**: ドメイン名を登録・貸し出す業者(お名前.com / Cloudflare / Namecheap 等)。Cloudflareは原価販売で最安水準。
- **DNS**: 「ドメイン→どのサーバか」を結ぶ住所録の仕組み。ここを設定して独自ドメインをホスティングに向ける。
- **ネームサーバ**: そのドメインのDNSをどの会社で管理するかの指定先。

## SEO / 収益化(テーマ検討で登場)
- **検索ボリューム**: あるキーワードが月に何回検索されるか。需要の大きさの指標。
- **キーワード難易度 / 競合性**: そのキーワードで検索上位を取る難しさ。ライバルサイトの強さ。検索ボリュームが大きい語ほど高い傾向。
- **ドメインオーソリティ**: サイト(ドメイン)全体の検索的な信用度。新規サイトは低く、実績の蓄積で上がる。低いほど激戦ワードで勝ちにくい。
- **ロングテール**: 「失業保険 自己都合 計算 30代」のような具体的で長めの検索語。1語あたりの需要は小さいが競合が手薄で、新規サイトが勝ちやすい。
- **CPC (Cost Per Click)**: 広告1クリックあたりの単価。AdSense収益のもと(数円〜数十円)。
- **成果報酬 / CPA (Cost Per Action)**: アフィリエイトで、申込・購入など「行動」が起きたときに発生する報酬(数百円〜数万円)。
- **E-E-A-T**: Experience(経験)・Expertise(専門性)・Authoritativeness(権威性)・Trust(信頼)。Googleがコンテンツ品質を測る観点。本人の専門性で担保する。
- **AdSense**: Googleの広告配信サービス。審査通過後にページへ広告を載せ、表示・クリックで収益。本プロジェクトの最終ゴールの計上先。
- **アフィリエイト**: 紹介リンク経由で読者が申込・購入すると成果報酬が入る仕組み。本領域では転職サービスが高単価。
- **ASP (Affiliate Service Provider)**: 「広告を出したい企業」と「サイト運営者」を仲介する市場。無料登録すると多数の広告主(案件)から選んで**自分専用の紹介リンク**を発行でき、成果が出ると報酬を計測・支払いまで担う。企業と1社ずつ契約せずに済む。流れ=①無料登録 ②案件に提携申請(承認制あり) ③リンクを記事に貼る(`rel="sponsored"`) ④成果→確定→振込(最低支払額あり)。**多くは登録審査で実サイトを見るため、原則は公開後/公開直前に登録**。主要ASP=A8.net(最大手・登録ゆるい)/アクセストレード(金融・転職に強く本ジャンル向き)/もしも・バリューコマース(物販)。
- **YMYL (Your Money or Your Life)**: お金・健康・法律など人生やお金を左右するテーマ。Googleが特に高いE-E-A-Tと情報の正確さを要求する。退職金・失業保険・税金が該当。一次情報に基づき出典明記で対応する。
- **商用利用**: そのサイトでお金を稼ぐこと。広告(AdSense)を貼ると「商用」扱いになる。Vercel Hobby(無料)は非商用向けのため、AdSense本番は Cloudflare Pages 等(無料・商用OK)へ移す。
- **トピックオーソリティ (topical authority)**: 「この分野ならこのサイト」とGoogleに認識される状態。関連する話題を1ドメインに束ねるほど強くなる。だから関連テーマは分散させず集約する。
- **内部リンク**: サイト内のページ同士をリンクで結ぶこと。サイトのまとまり・専門性をGoogleに伝える重要シグナル。1ドメインに同居させるなら必ず関連づけて結ぶ(つなげない同居は無意味)。
- **サイトを束ねる3つの軸**: ①テーマ軸(何の話か)②人物ブランド軸(誰が書くか=authorship/E-E-A-T)③オーディエンス軸(同じ読者の悩み・旅)。新規ドメインは①が立ち上げに効きやすく、③でまとめると関連テーマを一貫させつつ1ドメインで完結できる。本サイトは③「会社員から独立する人」軸。
- **サブディレクトリ**: 同一サイト内の下階層(例 `example.com/taishoku/`)。関連テーマを増やすときは新ドメインや別サイトでなく、ここに記事を足す(追加費用0円)。

## CMS / コンテンツ制作技術(技術スタック検討で登場)
- **CMS (Content Management System)**: 管理画面から記事を「書く・直す」ためのソフト。WordPress が代表格(世界のサイトの約4割)。
- **WordPress**: 最も普及したCMS。記事は書きやすいが、PHP/MySQLが動く有料サーバが必要(年1万円超になりがち)で、計算ツールは結局カスタム開発。学習が「管理画面操作」寄りになる。
- **Next.js**: 本プロジェクトの指定スタック。Reactベースのフレームワークで、記事も計算ツールも一つの土台で作れ、無料ホスティング(Vercel/Cloudflare)と相性が良い。Web技術の中身を学ぶのに最適。記事はMarkdownで書く。
- **ヘッドレスCMS**: 記事だけを外部の管理画面で書き、表示はNext.js等が担う仕組み。「管理画面で記事を書きたい」欲を、無料ホスティングのまま後から足せる。最初はMarkdown、必要になったら拡張、が定石。

## A-1 設計(情報設計・URL・レンダリング)
- **情報設計**: サイトに載せる情報をどう分け、どうつなぐかの設計図(家の間取り図にあたる)。記事を書く前に決める。
- **ハブ&スポーク(トピッククラスター)**: 中心の「ハブ(柱ページ。テーマ全体を見渡す+中核ツールを置く玄関)」と、論点を深掘りした「スポーク(個別記事)」を、内部リンクで相互に結ぶ設計パターン。①Googleに専門サイトと認識させる(トピックオーソリティ)②評価を内部で循環③読者を回遊させ収益導線へ、の3効果。
- **ハブの階層化(スケーラブルな分類)**: ハブは樹形図で入れ子にできる。スポークが記事10本級に育ったらサブハブに昇格させてよい。最初から完璧に切らず「育ったら割る」。
- **URL設計**: ハブ&スポークの構造を各ページの住所(URL)に反映させること。原則:サブディレクトリで階層化(`/ハブ/スポーク/`)、短く小文字ハイフン区切り、一度決めたら変えない(変更は評価リセット&リンク切れ)。本サイトは将来ハブが並列で入る形で確定。
- **スラッグ (slug)**: URL末尾のページ名部分(例 `.../shitsugyo-hoken/` の `shitsugyo-hoken`)。日本語URLは文字化けするためローマ字/英語で付ける。本サイトはローマ字で統一。
- **レンダリング**: ブラウザに見せる完成HTMLを生成すること。「いつ・どこで作るか」で4方式に分かれ、SEOとホスティング費用に直結する。
- **SSG (Static Site Generation)**: ビルド時にHTMLを作り置きする方式。速い・無料ホスティングと好相性・中身入りHTMLなのでSEO最強。更新の少ない記事ページ向き。**本サイトの記事はこれ**。
- **ISR (Incremental Static Regeneration)**: SSGの作り置きを一定間隔/オンデマンドで作り直す方式。「全員同じ内容 × 定期更新 × ページ大量」(ニュース・大規模EC商品・ランキング)向き。本サイトは当面不要、必要になったら導入。
- **SSR (Server-Side Rendering)**: アクセスのたびにサーバーでHTMLを生成。「人ごとに違う(ログイン後)/秒単位で最新が必要(在庫・検索結果)」をHTMLに載せたいとき向き。本サイトは登場しない見込み。
- **CSR (Client-Side Rendering)**: 訪問者のブラウザがJSでHTMLを組み立てる方式。中身が後出しなのでSEOに弱いが、非公開・インタラクティブな画面なら十分。**本サイトの中核ツール(結果はnoindexで動的表示)はこれ**。

## A-2 実装(Next.js の基本)
- **create-next-app**: Next.js プロジェクトの雛形を生成する公式ツール。`node_modules`(依存パッケージ、数百MB・gitignore対象)も導入する。既にGitリポジトリ内だとgit初期化はスキップする。
- **App Router**: Next.js の最新のページ構成方式。**`app/` フォルダの構成がそのままURLになる**(ファイルシステムルーティング)。
- **ファイルシステムルーティング**: フォルダ=URLの階層、`page.tsx`=そのURLで表示する中身。例 `app/taishoku/shitsugyo-hoken/page.tsx` → `/taishoku/shitsugyo-hoken/`。URL設計をそのまま再現できる。
- **`page.tsx`**: 特定URLで表示されるページ本体(Reactコンポーネントを default export)。
- **`layout.tsx`**: 複数ページで共有する枠(ヘッダー/フッター等)。ルートの `app/layout.tsx` は必須で `html`/`body` を含む。
- **`<Link>`(next/link)**: ページ間を移動する内部リンク部品。先読み(prefetch)で表示が速い。ハブ⇄スポークの配線に使う。
- **`metadata`**: 各ページの `export const metadata` でタイトル・説明文(SEOの基本情報)を指定。ルートで `title.template`(`%s | サイト名`)を設定すると各ページ名が自動で整う。
- **`robots: { index: false }`(noindex)**: そのページを検索結果に載せない指定。中核ツール(結果は動的)に付け、doorway/薄いページ回避の方針を実装。
- **`trailingSlash`**: URL末尾スラッシュの扱い。`true` で `/taishoku/` を正とし、静的書き出し時に `taishoku/index.html` を生成(静的ホスティング向き)。
- **TypeScript / `.tsx`**: JavaScript + 型チェック。`.tsx` はReactのJSXを含むTypeScriptファイル。書き間違いを実行前に検知。
- **CSR(再掲)**: ブラウザ側でJSが組み立てる方式。中核ツール(入力→計算→結果)はこれで実装予定。

## A-2 実装(デザイン・CSS / ゲート②)
- **デザイントークン (design token)**: 色・余白・角丸・影など「デザインの最小単位」を名前付きの値として一元管理する考え方。例:深緑=`--green`。1か所直せば全ページに反映でき、ブレを防ぐ。
- **CSS変数(カスタムプロパティ)**: `--green: #14452F;` のように CSS 内で定義し `var(--green)` で使い回せる変数。デザイントークンの実装手段。本サイトは `globals.css` の `:root` に集約。
- **グローバルCSS / CSSモジュール**: グローバルCSS=サイト全体に効くCSS(共通の土台向き)。CSSモジュール=ファイルごとに名前が衝突しないよう自動でスコープされるCSS(部品単位向き)。本サイトは当面グローバルCSS1枚＋クラスで運用。
- **再利用コンポーネント**: ボタン・カード・広告枠・著者枠など、見た目と役割をまとめた部品(`app/components/`)。各ページはこれを組み合わせるだけにして、重複と修正漏れを減らす。
- **next/font**: Next.js のフォント最適化機能。Webフォントを自前配信(self-host)し、表示ズレ(レイアウトシフト)を防ぐ。日本語は容量が大きいので `preload: false` にして必要時のみ読み込む。
- **可変フォント (variable font)**: 1ファイルで複数の太さを表現できるフォント。Noto Sans JP もこれ。読み込みが軽く柔軟。
- **`word-break: auto-phrase`**: 日本語を「文節」の切れ目で自然に折り返す新しいCSS指定。対応ブラウザで見出しの改行が読みやすくなる(非対応でも通常折り返しに戻るだけ)。
- **`clamp()`**: `clamp(最小, 可変, 最大)` で画面幅に応じて文字サイズ等をなめらかに変える指定。見出しをPCで大きくスマホで小さく、を1行で表現。
- **レスポンシブ / モバイルファースト / メディアクエリ**: 画面幅に応じてレイアウトを変える設計。モバイルを基本に組み、`@media (min-width: ...)` (メディアクエリ)で広い画面用に上書きする。本サイトはカードが狭い画面で縦積みになる。
- **パンくず (breadcrumb)**: ページ上部の「ホーム / 退職 / 失業保険」のような現在地表示。回遊性とSEO(階層の明示)に効く。
- **目次 (TOC) / アンカーリンク**: 記事冒頭の見出し一覧。各見出しに付けた `id` へ `#id` で飛ぶ内部リンク(アンカー)で実現。長文の拾い読みを助ける。
- **`rel="sponsored"`**: 広告・アフィリエイトのリンクに付ける属性で、「これは広告リンク」と検索エンジンに明示する(Google推奨・規約順守)。外部リンクには `rel="noopener"`(別タブで開く際の安全対策)も付ける。
- **BLUF (Bottom Line Up Front)**: 結論を最初に書く構成。記事冒頭で「要点3つ」を先出しして離脱を防ぐ。本サイト記事の標準。

## A-3 公開・技術SEO
- **構造化データ / JSON-LD**: ページの内容を「これは記事/著者/パンくず」と**機械可読**で検索エンジンに伝える仕組み。`schema.org` の語彙を使い、`<script type="application/ld+json">` でページに埋め込む。検索結果のリッチ表示(著者・日付など)に繋がる。
- **schema.org**: 構造化データの共通語彙(型)の定義集。`Article`(記事)・`BreadcrumbList`(パンくず)・`WebSite`・`Person`・`Organization` などを使う。
- **リッチリザルト (rich results)**: 通常の青リンクに加え、評価の星・FAQ・日付などが付いた検索結果の見た目。構造化データが前提。Googleの「リッチリザルトテスト」で検証できる。
- **canonical(正規URL)**: 「このページの“正式なURLはこれ”」を検索エンジンに伝える指定(`<link rel="canonical">`)。末尾スラッシュ違いやパラメータ付きで**同じ内容が複数URLになる重複**を1つに集約し、評価の分散を防ぐ。
- **metadataBase**: Next.js で相対パス(`/about/`)を絶対URL(`https://…/about/`)へ自動変換する基準URL。canonical・OGP・sitemap がこれを使う。本サイトは `app/lib/site.ts` の `SITE_URL` に集約。
- **sitemap.xml**: サイト内の全(インデックス対象)ページの一覧を検索エンジンに提出する地図ファイル。各URLに最終更新日・更新頻度・優先度を添える。Next.js では `app/sitemap.ts` で生成。
- **robots.txt**: クローラー(巡回ロボット)に「どこを巡回してよいか」と sitemap の場所を伝えるファイル。Next.js では `app/robots.ts` で生成。**noindex で隠したいページは robots で巡回を止めない**(止めると noindex を読めない)。
- **OGP(再掲・実装)**: SNS共有時のカード表示。`og:title`/`og:description`/`og:image`/`og:type`(記事は `article`)等。Next.js では `metadata.openGraph` で出力。ページが `openGraph` を設定すると親の設定を**置き換える**(継承されない)ので、`siteName` 等は再指定する。
- **環境変数 (environment variable)**: コードに直書きせず、デプロイ先ごとに外から渡す設定値。`NEXT_PUBLIC_` 始まりはブラウザにも露出する。本サイトは公開ドメインを `NEXT_PUBLIC_SITE_URL` で上書きできる。

## (以降、B 以降で出てくる用語をここに追記)
<!-- 例: GA4 / Search Console / Core Web Vitals / インデックス登録 / ImageResponse(OG画像) ... -->
